export const metadata = {
  title: "Add Product - HCH",
  description: "Welcome to Hot Coffee Hub, your daily dose of handcrafted coffee.",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}